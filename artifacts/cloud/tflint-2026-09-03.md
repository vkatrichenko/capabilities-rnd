# tflint — every Terraform root, tflint TFLint version 0.61.0 2026-09-03T20:38Z

Read-only; run over the same `git archive` snapshots as the Trivy scan (`scratch/cloud/src/<repo>/.MEASURED_REF`).
tflint is a *correctness/style* linter with an AWS ruleset (invalid instance types, deprecated arguments, missing pins) — not a
security scanner. It is here because two of the three IaC repos ship a `.tflint.hcl` and the question was whether it runs.
Raw JSON in `scratch/cloud/tflint/` (gitignored).

## What running each repo's own config showed

| Repo / root | Config used | Result |
|---|---|---|
| barley (`.tflint.hcl`, aws ruleset 0.39.0, `recommended`) | the repo's own | **0 issues, 0 errors** — consistent with `terraform-validate.yml` running it on every PR |
| hops IaC `terraform/us-east-2` (`.tflint.hcl`, aws plugin **0.22.1**, `module = true`) | the repo's own | **cannot load**: `"module" attribute was removed in v0.54.0` — and with that line patched, `Plugin "aws" SDK version (0.15.0) is incompatible. Compatible versions: >= 0.16.0`. Two independent incompatibilities with any tflint released since 2024: the file has not been executed in a long time, matching the repo having no CI |
| hops IaC, compatibility run (same rules, aws plugin 0.39.0, `call_module_type = "local"` — registry modules need `terraform init`, which this research does not run) | patched copy `scratch/cloud/tflint-hops-compat.hcl` | see table below |
| sow-insights-infra `941000539201-us-east-1` and `develop` (no `.tflint.hcl` in repo) | `scratch/cloud/tflint-default.hcl` (`recommended` + aws 0.39.0) | see table below |


## hops IaC (compat run)

issues 51 · errors 0

| Rule | Severity | Count | Example |
|---|---|---|---|
| terraform_required_providers | warning | 26 | `modules/acm-certificates/main.tf:35` Missing version constraint for provider "aws" in `required_providers` |
| terraform_required_version | warning | 20 | `modules/acm-certificates/main.tf:1` terraform "required_version" attribute is required |
| terraform_unused_declarations | warning | 2 | `modules/chatbot-slack/variables.tf:43` variable "chatbot_role_allow_notifications" is declared but not used |
| terraform_naming_convention | info | 2 | `modules/hops-rds/main.tf:9` resource name `hops-db-sg-ingress` must match the following format: snake_case |
| aws_iam_role_invalid_permissions_boundary | error | 1 | `modules/chatbot-slack/main.tf:63` permissions_boundary must be 20 characters or higher |

## sow-insights-infra · 941000539201-us-east-1

issues 21 · errors 0

| Rule | Severity | Count | Example |
|---|---|---|---|
| terraform_required_version | warning | 9 | `modules/acm_certificate/main.tf:1` terraform "required_version" attribute is required |
| terraform_required_providers | warning | 9 | `modules/acm_certificate/main.tf:6` Missing version constraint for provider "aws" in `required_providers` |
| terraform_unused_declarations | warning | 3 | `modules/acm_certificate/main.tf:1` data "aws_route53_zone" "this" is declared but not used |

## sow-insights-infra · develop

issues 12 · errors 0

| Rule | Severity | Count | Example |
|---|---|---|---|
| terraform_unused_declarations | warning | 7 | `modules/core/data-source.tf:1` data "aws_region" "us-east-1" is declared but not used |
| terraform_required_version | warning | 3 | `config.tf:1` terraform "required_version" attribute is required |
| terraform_required_providers | warning | 2 | `modules/core/vpc.tf:34` Missing version constraint for provider "aws" in `required_providers` |

## barley

issues 0 · errors 0

| Rule | Severity | Count | Example |
|---|---|---|---|
